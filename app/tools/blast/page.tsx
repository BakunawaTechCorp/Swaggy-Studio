import { AppPageStub } from "@/components/layout/AppPageStub";

export default function Page() {
  return (
    <AppPageStub
      title="Text + Email Blast"
      subtitle="Coming in Phase 5 — bulk send to your customer list."
    />
  );
}
