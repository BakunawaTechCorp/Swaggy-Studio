import { AppPageStub } from "@/components/layout/AppPageStub";

export default function Page() {
  return (
    <AppPageStub
      title="PR Projects"
      subtitle="Coming in Phase 2 — track outlets, deadlines, assets."
    />
  );
}
